package com.gimbal.rtn;

import com.facebook.react.bridge.ReactApplicationContext;

abstract class GimbalSpec extends NativeGimbalSpec {
    GimbalSpec(ReactApplicationContext context) {
        super(context);
    }
}
